To start the server type "node server" from the command line.

A config file must be added to run the server, in the name "config.json". It should match the format of the file "configFormat.json"

The conig file controls:
- the root folder from which files are served
- the context root (the path prefix before the file path)
- the port